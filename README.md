# ProgrammiLinguini
Projects for the Programming Languages class :)


# Tests
The tests are made with pytest, to install pytest type the following in your terminal
`$ pip3 install --user pytest`

To run the tests, run the next command at the root directory
`$ python3 -m pytest <test_file>`
where <test_file> is the test to run